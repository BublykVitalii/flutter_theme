import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white;
  static const background = Color.fromRGBO(15, 76, 131, 1);
  static const accent = Color.fromRGBO(250, 255, 0, 1);
  static const backgroundBottom = Color.fromRGBO(3, 43, 80, 1);
}
